// Este módulo debe importarse primero en cualquier punto de entrada
// para garantizar que las variables de entorno estén disponibles
// antes de que se evalúe cualquier otro código
import dotenv from 'dotenv';

dotenv.config();

